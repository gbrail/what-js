plugins {
    id 'jwhat.conventions'
    id 'application'
}

dependencies {
    implementation project(':core')
    implementation 'org.jline:jline:3.30.6'
}

application {
    mainModule = 'org.brail.jwhat.cli'
    mainClass = 'org.brail.jwhat.cli.CLI'
}

run {
    standardInput = System.in
}